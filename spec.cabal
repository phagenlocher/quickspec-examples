cabal-version: 2.2
name: spec
version: 0.1
build-type: Simple

common common-all
  default-language: Haskell2010
  build-depends:
    base == 4.*,
    QuickCheck,
    quickspec

library
  import: common-all
  hs-source-dirs: src/
  exposed-modules: Main
